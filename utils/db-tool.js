import mysql from "mysql";

const db = mysql.createConnection();

class DatabaseTool {
  static insertPageVisit(name, path) {
    return;
  }

  static insertSocialsVisit(name) {
    return;
  }
}

export default DatabaseTool;
